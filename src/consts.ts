export const root = document.documentElement;
export interface MagnetOpt {
  id: string;
  x: string;
  y: string;
  height: string;
  width: string;
  zIndex: string;
  text: string;
}
export interface FridgeOpt {
  id: number;
  name: string;
  count: number;
  current: number;
}
